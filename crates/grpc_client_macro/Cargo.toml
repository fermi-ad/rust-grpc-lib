[package]
name = "grpc-client-macro"
version = "0.1.0"
description = "Provides the macro for auto-deriving the internal GrpcClient trait on generated tonic structs"
edition = "2024"
publish = false

[lib]
proc-macro = true

[dependencies]
quote = "1.0.45"
syn = "2.0.117"
